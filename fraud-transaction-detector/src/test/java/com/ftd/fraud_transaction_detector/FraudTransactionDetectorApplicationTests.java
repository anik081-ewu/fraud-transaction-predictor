package com.ftd.fraud_transaction_detector;

import org.junit.jupiter.api.Test;

class FraudTransactionDetectorApplicationTests {

	@Test
	void placeholder() {
		// Keep a simple test so the project has at least one passing test
		// without requiring a database connection in CI/local dev.
	}

}
